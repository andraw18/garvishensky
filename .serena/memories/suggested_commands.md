# Suggested commands

The project does not appear to have a build system or test runner.

## Development
- Open the repository in VS Code and use Live Server to preview the site.
- Homepage: `index.html`
- Services page: `pages/services.html`

## Useful shell commands
- `rg --files .` - list project files
- `rg -n "pattern" index.html assets pages` - search within the site files
- `git status --short` - check repository status if the folder is inside a git checkout

## Notes
- No `npm`, `yarn`, `pnpm`, `vite`, or similar project commands were detected.
